from fastapi import FastAPI
app = FastAPI()
#fastapi is a library or a module and FastAPI is a class in that library. 
#We are creating an instance of the FastAPI class and assigning it to the variable app.
